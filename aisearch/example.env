# TruthValidator AI Search Configuration
# Copy this file to .env and update values

# Debug Mode (1=enabled, 0=disabled)
# WARNING: Debug mode exposes sensitive info in logs
DEBUG=1

# Server Listening Address
# Format: IP:PORT or :PORT (for all interfaces)
# Default: Localhost only for security
LISTEN_ADDR=127.0.0.1:1234

# OpenAI API Configuration
# Required for LLM processing
OPENAI=your_api_key_here
# Alternative OpenAI-compatible endpoint
# Example: "https://api.openai.com/v1"
OPENAI_ENDPOINT=

# SearxNG Configuration (Required)
# URL of your SearxNG instance
SEARXNG_URL=http://localhost:8888

# Security Recommendations:
# 1. Never commit .env to version control
# 2. Use environment variables for production secrets
# 3. Restrict LISTEN_ADDR in production
